# Optional initial model only; users can choose an AI model in workspace settings.
# Options: chat-latest, gpt-5.6-luna, gpt-5.6-terra, gpt-5.6-sol, gpt-6-astra.
# Do not put user API keys here.
# Users enter their own key in workspace settings for the current visit.
# OPENAI_API_KEY is not read; each request must supply its own credential.
OPENAI_MODEL=chat-latest
